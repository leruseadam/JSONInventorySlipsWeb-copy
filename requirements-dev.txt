# Base requirements
-r requirements.txt

# Development dependencies
pytest==7.4.0
black==23.7.0
flake8==6.1.0
mypy==1.5.1

# Explicitly exclude problematic packages
docxtpl<0  # This ensures docxtpl is uninstalled if present
docxcompose<0  # This ensures docxcompose is uninstalled if present
